{{c.name}} <!-- {{c.value}} --> <br/>
{{input type="range" min=c.restrictions.minimumValue max=c.restrictions.maximumValue value=(mut c.value) }}

<!--paper-slider min=c.restrictions.minimumValue max=c.restrictions.maximumValue value=c.value}}-->
